1. npm install -g react-native-cli
2. react-native init InstaluraMobile

Antes deve-se abrir o emulado do android studio.

3. react-native start
4. em outra aba do terminal executar o comando react-native run-android

